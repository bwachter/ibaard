#ifndef IBAARD_CAT_H
#define IBAARD_CAT_H

/** @file
 * A minimalistic rescue shell
 */

/** Initialize a minimalistic rescue shell
 *
 * @param info a message to display on shell launch
 * @return always returns 0
 */
int ash_init(char *info);

#endif
